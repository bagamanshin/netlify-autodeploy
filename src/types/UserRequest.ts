export type UserRequest = {
  // eslint-disable-next-line
  first_name: string,
  // eslint-disable-next-line
  second_name: string,
  // eslint-disable-next-line
  display_name: string,
  login: string,
  email: string,
  phone: string
}
