export type UserResponse = {
  id: number;
  // eslint-disable-next-line
  first_name:	string;
  // eslint-disable-next-line
  second_name:	string;
  // eslint-disable-next-line
  display_name:	string;
  login: string;
  email: string;
  phone: string;
  avatar: string;
}
